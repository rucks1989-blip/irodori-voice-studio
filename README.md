# irodori voice studio

**Windows向けローカル音声生成ツール（開発中）**

Irodori-TTS（audio.cpp版）をブラウザーから手軽に利用するためのローカルUIです。

GTX 1070（VRAM 8GB）環境での動作を確認しており、長文の自動分割、話者切替、参照WAV管理、無音挿入、WAV結合までをまとめて行えます。

通常の音声生成にはLLMは不要です。初心者向け視点でcodexとchatgpt使って開発しています。

<img width="1265" height="712" alt="image" src="https://github.com/user-attachments/assets/f0adebbe-a024-4644-a8a9-d639ddcc96cd" />


## 主な特徴

- Windows向けローカルUI
- LLMなしで音声生成可能
- ChatGPT・Gemini・Claudeなどで作成した文章をそのまま音声化
- 話者タグによる参照WAV自動切替
- Irodori-TTS向け長文自動分割
- 分割プレビュー
- WAV結合・無音挿入
- REST API対応
- GTX 1070（VRAM 8GB）で動作確認済み

## LLMは必須ではありません

このソフト単体で音声生成できます。

例えばChatGPTやGeminiで作成した文章をそのまま貼り付けるだけで利用できます。

```text
【佐藤】こんにちは。

【花子】おはようございます。

【佐藤】今日はいい天気ですね。
```

話者タグに対応する参照WAVを登録しておけば、自動で話者を切り替えて読み上げます。

## 長文自動分割

Irodori-TTSは長時間の連続生成では品質が低下する場合があります。

本ソフトでは、次の基準で自動分割し、長文でも自然につながるよう設計しています。

- 約120文字を目安
- 句読点を優先
- 不自然になりにくい位置

## セットアップ

### 必要なもの

- Windows 10 / 11
- Python 3
- audio.cpp
- Irodori-TTS GGUFモデル
- 参照WAV（任意）

初回のみ `setup.bat` を実行してください。

セットアップ完了後は `start.bat` で起動できます。

自動導入できなかった項目は `download_links.html`、詳しい補足は `SETUP_MANUAL.txt` を参照してください。

## 導入例・検証結果

これは導入例です。

### 検証1：初回自動セットアップ

**事前に存在したもの**

- Windows 11
- Python 3
- NVIDIAドライバー
- インターネット接続

**存在しなかったもの**

- audio.cpp runtime
- Irodori-TTS GGUFモデル
- 参照WAV
- LLM
- ローカル設定
- 生成履歴

**結果**

- `setup.bat` による公式CPU版audio.cppの取得：成功
- Irodori-TTSモデルの取得とハッシュ検証：成功
- `start.bat` からのUI起動：成功
- CPU音声生成：成功
- 短文の初回生成：約19秒

### 検証2：GTX 1070向け外部CUDAバックエンド

- GTX 1070
- VRAM 8GB
- Pascal対応として別途用意したaudio.cpp
- GPU音声生成：成功

## 初心者向けサポート

`setup.bat` の実行後には、セットアップ結果画面が表示されます。

画面では現在のPC環境を自動で確認し、次の情報などを一覧で確認できます。

- OS
- GPU
- NVIDIAドライバーバージョン
- Compute Capability
- 現在使用している音声バックエンド（CUDA / CPU）
- 導入済みの項目
- 未達成・推奨項目
- 任意で追加できる機能

また、**「未達成項目をAI相談用にコピー」**ボタンを用意しています。

このボタンを押すと、現在のPC環境と不足している項目をまとめた質問文がクリップボードへコピーされます。

そのままChatGPT、Gemini、ClaudeなどのAIへ貼り付けるだけで、次の内容を現在の環境に合わせて案内してもらえます。

- 何が必須なのか
- 何が任意なのか
- 自分のPCでは何を追加すればよいのか
- 初心者向けの導入手順

コピーされる内容には、個人名・ユーザー名・絶対パスなどの個人情報は含まれません。安全に質問できるよう配慮しています。

## 基本的な使い方

1. 原稿を入力
2. 必要なら参照WAVを選択
3. 「分割を確認」でチャンクを確認
4. 「音声を生成」
5. 再生または保存

参照WAVを選択しない場合は、Irodori-TTS内部の音声が使用される場合があります。

初回生成はモデルや参照音声の読み込みにより時間がかかることがあります。

## 話者切替

話者切替画面で、次のように対応付けを登録します。

```text
【佐藤】 → satou.wav
【花子】 → hanako.wav
```

原稿中の話者タグは読み上げず、登録した参照WAVへ自動的に切り替わります。

## 読まない文字列

指定した文字列を音声生成前に削除できます。

作品独自の注釈やラベルなど、読み上げ不要な文字列に利用できます。

## オプション：キャラクター会話

通常の音声生成にLLMは不要ですが、「会話」タブではOpenAI互換Chat APIを利用できます。

例：

```text
http://127.0.0.1:11438/v1/chat/completions
```

LLMの返答も通常の音声生成処理へ渡されるため、次の機能をそのまま利用できます。

- 自動分割
- 話者切替
- 無音挿入
- WAV結合

## GPUについて

GPU対応はaudio.cppのビルドに依存します。

現在は、次の両方に対応しています。

- CPU版
- CUDA版

GTX 1070（Pascal・VRAM 8GB）環境で動作を確認しています。

ローカルLLMを利用する場合は、次の運用を想定しています。

```text
LLM起動
↓
文章生成
↓
LLM終了（VRAM解放）
↓
Irodori-TTSで音声生成
```

この方式により、VRAMが少ない環境でもLLMと音声生成を切り替えながら利用できます。

## ライセンス

audio.cpp、Irodori-TTSモデル、Pythonパッケージ、CUDAなどは、それぞれ別ライセンスです。

利用できることと、本リポジトリから再配布できることは同じではありません。

正式公開前には必ず各配布元のライセンスを確認してください。

詳細は `LICENSE` および `LICENSES.md` を参照してください。

## 音声利用上の注意

- 利用権限のある参照WAVのみ使用してください。
- 本人の許可がないなりすまし、詐欺、誤情報、嫌がらせなどへの利用は禁止します。
- 生成物を公開・販売する場合は、参照音声・モデル・素材・公開先の利用規約を確認してください。
- 生成した音声は必ず人間が内容を確認してください。

## 不具合報告

不具合を報告する場合は、個人情報や参照WAV、モデル本体は添付せず、可能な範囲で次の情報を記載してください。

- Windowsのバージョン
- GPU名とドライバーバージョン
- CPU版またはCUDA版のどちらを使用したか
- setup結果の未達成項目
- 再現手順
- 個人情報を除いたエラーメッセージ

